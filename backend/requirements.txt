# -------------------------------------
# 1. Core Web Server & API
# -------------------------------------
fastapi
uvicorn[standard]
python-dotenv

# -------------------------------------
# 2. Authentication
# -------------------------------------
python-jose[cryptography]   # JWT signing / verification
passlib[bcrypt]             # password hashing
bcrypt==4.0.1               # pin: passlib ≥ 4.1 compat bug with bcrypt > 4.0
pydantic[email]             # EmailStr support (includes email-validator)
pydantic-settings           # typed settings from .env
httpx                       # OAuth HTTP calls (Google / GitHub)

# -------------------------------------
# 3. AI & Multi-Agent (LangGraph)
# -------------------------------------
langgraph
langchain
langchain-openai

# -------------------------------------
# 4. Database (PostgreSQL)
# -------------------------------------
sqlalchemy
asyncpg
alembic

# -------------------------------------
# 5. Game State & Caching
# -------------------------------------
redis