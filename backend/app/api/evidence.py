# backend/app/api/evidence.py
"""
Evidence router — placeholder until the evidence feature is implemented.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/evidence", tags=["Evidence"])
