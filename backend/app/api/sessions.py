# backend/app/api/sessions.py
"""
Sessions router — placeholder until the sessions feature is implemented.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/api/sessions", tags=["Sessions"])
